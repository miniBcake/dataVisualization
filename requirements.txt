# 데이터 처리를 위한 라이브러리
pandas>=2.2.3

# 데이터 시각화를 위한 라이브러리
matplotlib>=3.9.2

# 데이터 병합 및 경로 관리
numpy>=2.1.3

# Jupyter Notebook에서 실행을 위한 패키지 (옵션)
notebook>=7.2.2

# pip install -r requirements.txt (terminal 명령어)